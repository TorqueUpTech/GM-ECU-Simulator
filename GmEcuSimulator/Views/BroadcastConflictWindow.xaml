<Window x:Class="GmEcuSimulator.Views.BroadcastConflictWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resolve CAN ID conflicts"
        Height="480" Width="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FontSize="14"
        Style="{StaticResource Window.Dialog}">

    <DockPanel LastChildFill="True">

        <!-- Custom titlebar -->
        <Grid DockPanel.Dock="Top" Background="{DynamicResource Chrome.GradientBrush}" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Resolve CAN ID conflicts"
                       Style="{StaticResource Text.Body}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"/>
            <Button Grid.Column="1"
                    Style="{StaticResource Button.ChromeClose}"
                    Width="46" Height="32"
                    Click="OnCancelClicked"
                    ToolTip="Cancel">
                <Path Data="{StaticResource Icon.Close}"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.2"
                      Width="10" Height="10"/>
            </Button>
        </Grid>

        <!-- Footer -->
        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource Chrome.BgBrush}"
                BorderBrush="{DynamicResource Border.SubtleBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" IsCancel="True" MinWidth="90" Click="OnCancelClicked"/>
                <Button Content="OK"
                        IsDefault="True" MinWidth="110" Margin="10,0,0,0"
                        Click="OnOkClicked"/>
            </StackPanel>
        </Border>

        <!-- Explanation -->
        <Border DockPanel.Dock="Top" Padding="14,12,14,8">
            <StackPanel>
                <TextBlock Text="These CAN IDs already exist in the broadcast table, but this DBC defines them with a different shape."
                           Style="{StaticResource Text.Subtitle}" TextWrapping="Wrap"/>
                <TextBlock Text="An arbitration ID can only carry one frame. Tick Replace to take the imported definition (your existing row and its source mappings are discarded); untick to keep your existing row and ignore this DBC's version."
                           Style="{StaticResource Text.Caption}" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Conflict list -->
        <Border Margin="14,0,14,12" CornerRadius="4"
                BorderBrush="{DynamicResource Border.SubtleBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Conflicts}" Margin="8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,6" Margin="0,2"
                                    BorderBrush="{DynamicResource Border.SubtleBrush}" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding CanIdHex}"
                                               FontFamily="{StaticResource Font.Mono}"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock>
                                            <Run Text="Existing: " Foreground="{DynamicResource Text.SecondaryBrush}"/>
                                            <Run Text="{Binding ExistingLabel, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Margin="0,2,0,0">
                                            <Run Text="Imported: " Foreground="{DynamicResource Text.SecondaryBrush}"/>
                                            <Run Text="{Binding IncomingLabel, Mode=OneWay}" Foreground="{DynamicResource Accent.PrimaryBrush}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <CheckBox Grid.Column="2"
                                              Content="Replace"
                                              IsChecked="{Binding Replace}"
                                              VerticalAlignment="Center"
                                              Margin="12,0,4,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
